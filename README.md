# Datasciencelab1
introduction to keggle and worked on titanic dataset
create a notebbok in keggle,and click add input button button to add dataset 
now ,run the given code and after the give a command with df=pd.read_csv('file path')
and after tha we can give command like ifno,head,tail,describe to file(e.g. df.describe() ).
